programa {
  funcao inicio() {
    real salario
    escreva("coloque seu salário: ")
    leia(salario)

    real aumento = (salario * 0.1) + salario

    escreva("Seu resultado do salário é ", aumento, " R$")
  }
}
